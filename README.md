# allwhetherdashboard

Streamlit dashboard for ETF auto-investing simulation.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```
